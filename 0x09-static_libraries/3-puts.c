#include "main.h"
void_puts(char *str)
#include<stdio.h>
/**
 * main -Entry point
 *
 * Return:Always 0 (success)
 */
int main(void)
{
puts("\"programming is like building a multilingual puxxle");
return (0);
}
