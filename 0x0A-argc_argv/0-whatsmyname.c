#include <stdio.h>

/**
 * main - function thats outputs the string
 * @argc: integer
 * @argv: character
 * Return: 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])

{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}

